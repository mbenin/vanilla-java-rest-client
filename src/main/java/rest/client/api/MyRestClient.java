package rest.client.api;

public class MyRestClient extends AbstractBaseRestClient {

    public MyRestClient(String baseUrl, String username, String password) {
        super(baseUrl, username, password);
    }
}
